
services:
  web:
    build:
      context: ..
      dockerfile: docker/backend.Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../backend:/app
    command: python manage.py runserver 0.0.0.0:8000